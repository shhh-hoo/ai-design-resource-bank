import { visual, escape as esc } from "./visuals.js";
const main=document.querySelector("main");
const json=async(path)=>{const r=await fetch(path);if(!r.ok)throw new Error(`Unable to load ${path}`);return r.json();};
let indexPromise;const indexRecords=()=>(indexPromise||=json("catalog/index.json"));
const data=await json("catalog/web-index.json");const examples=new Map(data.examples.map(e=>[e.id,e]));const topics=new Map(data.topics.map(t=>[t.id,t]));const selected=new Map();let committed=null,routeVersion=0;
const titleize=value=>String(value).replaceAll("-"," ").replace(/\b\w/g,c=>c.toUpperCase());
const uniquePairs=values=>[...new Set(values.filter(Boolean))].sort().map(v=>[v,titleize(v)]);
const previewFor=e=>e.kind==="GAP"?'<span class="gap-sign">＋</span><span>Example needed</span>':e.demo_url?'<div class="preserved-preview"><span>Interactive study ↗</span></div>':visual(e.preview.renderer,e.title);
const card=e=>{if(!e)return"";const meta=e.kind==="GAP"?"Missing coverage":e.demo_url?"Preserved interactive study":e.kind==="LIVE"?"Interactive example":"Reference / study";return `<a class="card" href="#example/${e.id}"><div class="preview ${e.kind==="GAP"?"gap-preview":""}">${previewFor(e)}</div><h3>${esc(e.title)}</h3><div class="card-meta"><span class="kind-${e.kind.toLowerCase()}">${e.kind}</span><span>·</span><span>${meta}</span></div></a>`;};
const heading=(tag,title,copy,label="Design resource bank")=>`<div class="page-heading"><div><p class="eyebrow">${esc(tag)}</p><h1>${title}</h1><p class="intro">${copy}</p></div>${label?`<span class="subject-label">${esc(label)}</span>`:""}</div>`;
const list=arr=>`<ul>${arr.map(x=>`<li>${esc(x)}</li>`).join("")}</ul>`;const busy=()=>'<p class="loading">Loading…</p>';
function examplesView(){const available=data.examples.filter(e=>e.kind!=="GAP").sort((a,b)=>Number(Boolean(b.demo_url))-Number(Boolean(a.demo_url))||a.title.localeCompare(b.title));main.innerHTML=`<section class="bank-intro"><div><p class="eyebrow">THE RESOURCE BANK</p><h1>Examples worth seeing.</h1></div><p class="intro">Browse concrete references and live studies directly. Concepts, tools, subject mappings and implementation detail stay connected behind each example rather than replacing it.</p></section><div class="section-lead"><p>${available.length} visible examples · ${data.examples.filter(e=>e.demo_url).length} preserved interactive studies</p></div><div class="grid">${available.map(card).join("")}</div>`;}
function explore(){const filter=(id,title,values)=>`<label>${title}<select id="${id}" aria-label="${title}"><option value="">All</option>${values.map(([v,n])=>`<option value="${esc(v)}">${esc(n)}</option>`).join("")}</select></label>`;const media=uniquePairs(data.examples.map(e=>e.medium)),interactions=uniquePairs(data.examples.map(e=>e.interaction)),traits=uniquePairs(data.examples.flatMap(e=>e.visual_traits||[]));main.innerHTML=heading("VISUAL DISCOVERY","Follow your curiosity.","Browse the bank by what an example feels like, shows, or does — without needing to know its formal name.")+`<div class="filters">${filter("intent","Intent",data.intents.map(x=>[x.id,x.title]))}${filter("medium","Medium",media)}${filter("interaction","Interaction",interactions)}${filter("trait","Visual trait",traits)}${filter("collection","Collection",data.collections.map(x=>[x.id,x.title]))}${filter("availability","Kind",[["LIVE","LIVE"],["REFERENCE","REFERENCE"],["GAP","GAP"]])}</div><div class="grid explore" id="results"></div>`;const update=()=>{const value=id=>document.getElementById(id).value;const result=data.examples.filter(e=>(!value("intent")||(e.intent_ids||[]).includes(value("intent")))&&(!value("medium")||e.medium===value("medium"))&&(!value("interaction")||e.interaction===value("interaction"))&&(!value("trait")||(e.visual_traits||[]).includes(value("trait")))&&(!value("collection")||(e.collection_ids||[]).includes(value("collection")))&&(!value("availability")||e.kind===value("availability")));const rank={LIVE:0,REFERENCE:1,GAP:2};result.sort((a,b)=>(rank[a.kind]??9)-(rank[b.kind]??9)||a.title.localeCompare(b.title));document.getElementById("results").innerHTML=result.length?result.map(card).join(""):'<p class="empty">No examples match these filters.</p>';};main.querySelectorAll("select").forEach(s=>s.addEventListener("change",update));update();}
function subjectsView(){const roots=data.topics.filter(t=>!t.parent);main.innerHTML=heading("SUBJECT PACKS","Domain knowledge, without owning the bank.","Subject Packs preserve a domain's own knowledge map and coverage. They point into the same shared Examples, Concepts, Tools and Resources.")+`<div class="subject-section">${roots.map(root=>{const children=data.topics.filter(t=>t.parent===root.id);const count=new Set(children.flatMap(t=>t.example_ids)).size;return `<a class="subject-card" href="#subject/${root.id}"><h2>${esc(root.title)}</h2><p>${esc(root.scope||"")} · ${children.length} topics · ${count} mapped example slots</p></a>`;}).join("")}</div>`;}
function subjectView(subjectId="topic:chemistry"){const subject=topics.get(subjectId);if(!subject||subject.parent)throw new Error("Unknown Subject Pack");const children=data.topics.filter(t=>t.parent===subjectId).sort((a,b)=>(a.order??0)-(b.order??0));main.innerHTML=heading("SUBJECT PACK",`${esc(subject.title)}, mapped.`,"Browse this domain by its own knowledge structure. These topics organize examples; they do not define the rest of AIDRB.",`Subject pack / ${subject.title}`)+children.map(topic=>`<section class="subject-section"><header><div><p class="eyebrow">${esc(topic.id)}</p><h2>${esc(topic.title)}</h2></div><p>${esc(topic.scope||"")}</p></header><div class="grid">${topic.example_ids.map(id=>card(examples.get(id))).join("")}</div></section>`).join("");}
function atlas(topicId="topic:chemistry-energetics"){const topic=topics.get(topicId);if(!topic||!topic.parent)throw new Error("Unknown subject topic");const root=topics.get(topic.parent);main.innerHTML=heading("SUBJECT TOPIC",esc(topic.title),esc(topic.scope||"Browse mapped examples."),`Subject pack / ${root?.title||"Subject"}`)+`<div class="grid">${topic.example_ids.map(id=>card(examples.get(id))).join("")}</div>`;}
function collectionsView(){main.innerHTML=heading("COLLECTIONS","Curated ways into the bank.","Collections group useful references across type or medium without turning the grouping into a universal taxonomy.")+data.collections.map(collection=>{const items=data.examples.filter(e=>(e.collection_ids||[]).includes(collection.id));return `<section class="collection-section"><header><div><p class="eyebrow">${esc(collection.id)}</p><h2>${esc(collection.title)}</h2></div><p>${esc(collection.summary||"")}</p></header><div class="grid">${items.map(card).join("")||'<p class="empty">No mapped examples yet.</p>'}</div></section>`;}).join("");}
function sourceLabel(source){const linkable=["url","repository","paper","image","video"].includes(source.locator_type)&&/^https?:\/\//i.test(source.locator);const label=linkable?`<a class="external" href="${esc(source.locator)}" target="_blank" rel="noreferrer">${esc(source.title)} ↗</a>`:`<strong>${esc(source.title)}</strong>`;return `${label}<br><span class="source-locator">${esc(source.locator_type)} · ${esc(source.locator)}</span>`;}
async function detail(id,version){const e=examples.get(id);if(!e)throw new Error("Unknown Example");const topicId=(e.topic_ids||[])[0],topic=topicId?topics.get(topicId):null,backHref=topic?`#atlas/${topicId}`:"#examples",backLabel=topic?.title||"Examples",contextLabel=topic?`Subject / ${topics.get(topic.parent)?.title||"Subject"}`:"Resource bank";const stage=e.kind==="GAP"?'<span class="gap-sign">＋</span><span>GAP · A concrete example is still needed</span>':e.demo_url?`<iframe class="legacy-demo-frame" src="${esc(e.demo_url)}" title="${esc(e.title)} preserved interactive demo"></iframe>`:visual(e.preview.renderer,e.title);main.innerHTML=`<article class="detail"><a class="back" href="${backHref}">← ${esc(backLabel)}</a><div class="detail-stage ${e.kind==="GAP"?"gap-preview":""}" id="stage">${stage}</div><div class="detail-identification"><div><p class="eyebrow">${e.kind} / ${esc(contextLabel)}</p><h1>${esc(e.title)}</h1><p id="context">${e.kind==="GAP"?esc(e.gap_reason):""}</p><span class="data-id">${e.id}</span></div><button id="select-example" ${e.kind==="GAP"?"disabled":""}>${selected.has(id)?"Selected ✓":"Add to selection"}</button></div><details id="ai-build"><summary>AI Build</summary><div class="details-body">${busy()}</div></details><details id="implementation"><summary>Implementation & model limits</summary><div class="details-body">${busy()}</div></details><details id="provenance"><summary>Provenance${topic?" & curriculum crosswalks":""}</summary><div class="details-body">${busy()}</div></details></article>`;document.getElementById("select-example").onclick=event=>{selected.set(id,selected.get(id)||{id,aspect_notes:"",constraints:[]});committed=null;event.target.textContent="Selected ✓";count();};if(e.kind==="LIVE"&&!e.demo_url){const {mountLive}=await import("./live.js");if(version!==routeVersion)return;mountLive(document.getElementById("stage"),e.preview.renderer,e.title);}const resolved=await json(e.resolve_path);if(version!==routeVersion)return;const r=resolved.record,b=resolved.ai_build;document.getElementById("context").textContent=r.summary;document.querySelector("#ai-build .details-body").innerHTML=`<p>${r.kind==="GAP"?"Coverage gap: no implementation is claimed.":"Generated from this exact Example. Proposed alternatives are distinguished from observed implementation."}</p><h3>Method</h3><p>${esc(b.method.grammar.join(" · "))} / ${esc(r.interaction)}</p>${list(b.method.transfer_constraints)}<h3>Mechanism evidence</h3>${b.method.mechanisms.length?list(b.method.mechanisms.map(x=>`${x.id}: ${x.implementation_note} (${x.evidence})`)):"<p>No legacy mechanism required.</p>"}<h3>Tools</h3>${b.tools.map(t=>`<p><strong>${esc(t.record.title)}</strong> · ${t.evidence}<br>${esc(t.reason)}</p><p>Use when: ${esc(t.record.use_when)}<br>Avoid when: ${esc(t.record.avoid_when)}</p>`).join("")||"<p>No tool implementation claim.</p>"}<h3>Resources</h3>${b.resources.length?list(b.resources.map(x=>`${x.title} · ${x.id} · ${x.status}`)):"<p>No extracted Resource Package.</p>"}<p class="gap-note">Commit your selection before locking IDs and fetching full build resources.</p>`;document.querySelector("#implementation .details-body").innerHTML=`<h3>State model</h3><p>${esc(r.state_model.model)} · deterministic: ${r.state_model.deterministic}</p><p>${esc(r.interaction_model.response)}</p><h3>Fidelity constraints</h3>${list(r.fidelity_constraints)}<h3>Avoid</h3>${list(r.failure_modes)}<h3>Lifecycle</h3><p>${Object.entries(r.lifecycle).map(([k,v])=>`${k}: ${v?"yes":"no"}`).join(" · ")}</p>`;const crosswalks=resolved.crosswalks||[];const crosswalkHtml=crosswalks.length?`<p>Canonical topic → curriculum node. These editorial mappings describe scope, not approval by a curriculum board.</p>${crosswalks.map(c=>`<p><strong>${esc(c.node.label)}</strong> · ${esc(c.node.section)}<br>Canonical topic is <strong>${c.relation}</strong> relative to this source node.<br>${esc(c.rationale)}</p>`).join("")}`:"";document.querySelector("#provenance .details-body").innerHTML=`${crosswalkHtml}<h3>Sources</h3>${resolved.sources.map(s=>`<p>${sourceLabel(s)}<br>${esc(s.publisher)} · checked ${esc(s.checked_at||"not independently checked")}<br><span class="gap-note">${esc(s.check_scope)}</span></p>`).join("")}`;}
async function dictionary(version){main.innerHTML=heading("THE DICTIONARY","A name for what you see.","Concept vocabulary grounded in concrete examples from across the bank.")+'<div class="filters"><label>Find a concept<input id="concept-search" type="search" placeholder="Parallax, spotlight, reaction…"></label></div><div id="dictionary-results">'+busy()+"</div>";const concepts=await json("catalog/dictionary.json");if(version!==routeVersion)return;const render=()=>{const q=document.getElementById("concept-search").value.toLowerCase();const matches=concepts.filter(c=>(c.title+" "+c.summary).toLowerCase().includes(q)).sort((a,b)=>Number(b.example_ids.some(id=>examples.get(id)?.kind!=="GAP"))-Number(a.example_ids.some(id=>examples.get(id)?.kind!=="GAP"))||a.title.localeCompare(b.title));document.getElementById("dictionary-results").innerHTML=matches.map(c=>`<section class="dictionary-entry"><div><h2><a href="#record/${c.id}">${esc(c.title)}</a></h2><p>${esc(c.summary)}</p>${c.adjacent_ids.length?`<p>Adjacent: ${c.adjacent_ids.map(id=>`<a class="external" href="#record/${id}">${esc(id.replace("concept:","").replaceAll("-"," "))}</a>`).join(" · ")}</p>`:""}</div><div class="grid">${c.example_ids.length?c.example_ids.map(id=>card(examples.get(id))).join(""):'<p class="gap-note">No curated Example yet.</p>'}</div></section>`).join("")||'<p class="empty">No matching concepts.</p>';};document.getElementById("concept-search").addEventListener("input",render);render();}
async function index(version){main.innerHTML=heading("THE INDEX","Everything, by name.","Compact exhaustive lookup across the resource bank.")+'<div class="filters"><label>Search name or stable ID<input id="index-search" type="search" placeholder="Name or stable ID"></label><label>Entity type<select id="entity-type"><option value="">All types</option>'+["Concept","Example","Tool","Resource","SubjectTopic","Intent","Collection","Source"].map(t=>`<option>${t}</option>`).join("")+'</select></label></div><table class="index-table"><thead><tr><th>Type</th><th>Name</th><th>Stable ID</th></tr></thead><tbody id="index-results"></tbody></table>';const records=await indexRecords();if(version!==routeVersion)return;const render=()=>{const q=document.getElementById("index-search").value.toLowerCase(),kind=document.getElementById("entity-type").value;document.getElementById("index-results").innerHTML=records.filter(r=>(!kind||r.type===kind)&&(r.id+" "+r.title).toLowerCase().includes(q)).map(r=>`<tr><td>${r.type}</td><td><a href="#${r.type==="Example"?"example":"record"}/${r.id}">${esc(r.title)}</a></td><td class="data-id">${r.id}</td></tr>`).join("");};main.querySelectorAll("input,select").forEach(x=>x.addEventListener("input",render));render();}
function count(){document.getElementById("selection-count").textContent=selected.size;}function download(value,name){const url=URL.createObjectURL(new Blob([JSON.stringify(value,null,2)+"\n"],{type:"application/json"}));const a=document.createElement("a");a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
async function selection(version){main.innerHTML=heading("SHARED SELECTION","Choose what to carry forward.","Candidates stay editable. Commit preserves your exact references and notes.")+([...selected.values()].map(s=>`<section class="selection-item"><h3><a href="#example/${s.id}">${esc(examples.get(s.id).title)}</a></h3><label>Aspect notes<textarea data-notes="${s.id}" placeholder="What should be borrowed? What should be avoided?">${esc(s.aspect_notes)}</textarea></label><label>Constraints · one per line<textarea data-constraints="${s.id}">${esc(s.constraints.join("\n"))}</textarea></label><button data-remove="${s.id}">Remove</button></section>`).join("")||'<p class="empty">Open an Example and add it to your selection.</p>')+`<div class="selection-actions"><button class="primary" id="commit" ${selected.size?"":"disabled"}>Commit selection</button><button id="export-selection" disabled>Download committed selection</button></div><p class="notice" id="commit-status"></p>`;main.querySelectorAll("[data-notes],[data-constraints]").forEach(x=>(x.oninput=()=>{const s=selected.get(x.dataset.notes||x.dataset.constraints);if(x.dataset.notes)s.aspect_notes=x.value;else s.constraints=x.value.split("\n").filter(Boolean);committed=null;document.getElementById("export-selection").disabled=true;document.getElementById("commit-status").textContent="Selection changed. Commit again to freeze these choices.";}));main.querySelectorAll("[data-remove]").forEach(x=>(x.onclick=()=>{selected.delete(x.dataset.remove);committed=null;count();selection(version);}));document.getElementById("commit").onclick=async()=>{try{const {fingerprint}=await json("catalog/manifest.json");if(version!==routeVersion)return;const body={version:1,state:"committed",selection:{selector:"human",selections:structuredClone([...selected.values()])},catalog_fingerprint:fingerprint};const canonical=v=>Array.isArray(v)?v.map(canonical):v&&typeof v==="object"?Object.fromEntries(Object.keys(v).sort().map(k=>[k,canonical(v[k])])):v;const bytes=new TextEncoder().encode(JSON.stringify(canonical(body),null,2)+"\n");const digest=await crypto.subtle.digest("SHA-256",bytes);committed={...body,commit_digest:[...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,"0")).join("")};document.getElementById("export-selection").disabled=false;document.getElementById("commit-status").textContent="Committed. Download this selection, then use the retrieval CLI to lock these exact IDs and fetch build resources.";}catch(e){document.getElementById("commit-status").textContent=e.message;}};document.getElementById("export-selection").onclick=()=>download(committed,"aidrb-selection.json");}
async function record(id,version){if(!/^[a-z]+:[a-z0-9-]+$/.test(id))throw new Error("Invalid stable ID");main.innerHTML=busy();const entry=(await indexRecords()).find(r=>r.id===id);if(!entry)throw new Error("Unknown stable ID");const r=await json(entry.resolve_path);if(version!==routeVersion)return;main.innerHTML=`<article class="record"><a class="back" href="#index">← Index</a><p class="eyebrow">${r.record.type}</p><h1>${esc(r.record.title)}</h1><p class="intro">${esc(r.record.summary||r.record.scope||"")}</p><p class="data-id">${r.record.id}</p><pre>${esc(JSON.stringify(r,null,2))}</pre></article>`;}
async function route(){const version=++routeVersion;const [rawView,id]=location.hash.slice(1).split("/");const view=rawView||"examples";let active=view;if(view==="atlas"||view==="subject")active="subjects";if(view==="example")active=(examples.get(id)?.topic_ids||[]).length?"subjects":"examples";document.querySelectorAll("nav a").forEach(a=>{if(a.hash==="#"+active)a.setAttribute("aria-current","page");else a.removeAttribute("aria-current");});try{if(view==="examples")examplesView();else if(view==="explore")explore();else if(view==="subjects")subjectsView();else if(view==="subject")subjectView(id);else if(view==="atlas")atlas(id);else if(view==="collections")collectionsView();else if(view==="example")await detail(id,version);else if(view==="dictionary")await dictionary(version);else if(view==="index")await index(version);else if(view==="selection")await selection(version);else if(view==="record")await record(id,version);else throw new Error("Unknown page");if(version===routeVersion){const names={examples:"Examples",explore:"Explore",subjects:"Subjects",subject:"Subject Pack",atlas:"Subject Topic",collections:"Collections",dictionary:"Dictionary",index:"Index",selection:"Selection",record:"Record"};document.title=`AIDRB — ${view==="example"?examples.get(id).title:names[view]||"Design Resource Bank"}`;window.scrollTo(0,0);}}catch(e){if(version===routeVersion)main.innerHTML=`<h1>Unable to open this view.</h1><p>${esc(e.message)}</p><a href="#examples">Return to Examples</a>`;}}
window.addEventListener("hashchange",route);route();
