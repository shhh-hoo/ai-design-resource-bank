name: Deploy capability board

on:
  push:
    branches:
      - main
    paths:
      - index.html
      - dictionary.html
      - dictionary/**
      - web/**
      - registries/**
      - catalog.yaml
      - scripts/build_board_data.py
      - .github/workflows/deploy-board.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Compile runtime data
        run: |
          python -m pip install --disable-pip-version-check --quiet pyyaml
          mkdir -p _site/data
          python scripts/build_board_data.py --output _site/data/board.json

      - name: Build static board artifact
        run: |
          cp index.html _site/index.html
          cp dictionary.html _site/dictionary.html
          cp catalog.yaml _site/catalog.yaml
          cp -R dictionary _site/dictionary
          cp -R web _site/web
          cp -R registries _site/registries
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
